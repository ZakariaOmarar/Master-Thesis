"""Modeling package public surface for the V0–V5 pipeline.

Currently exposes only the V0 baselines.  V1+ will register additional public
symbols as the iterations land (encoders, fusion, anomaly head, conditional
localization head).
"""

from __future__ import annotations
